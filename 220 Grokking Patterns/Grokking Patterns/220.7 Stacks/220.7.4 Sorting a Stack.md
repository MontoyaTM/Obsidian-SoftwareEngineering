Status: 
Tag:
Links: [[220.7 Stacks]]

---
> [!note] 
>  # Sorting a Stack

Given a stack, sort it using only stack operations (push and pop).

You can use an additional temporary stack, but you may not copy the elements into any other data structure (such as an array). The values in the stack are to be sorted in descending order, with the largest elements on top.

![[Pasted image 20250310202340.png]]

> [!success] 
> ## Solution 

This problem can be solved by using a temporary stack as auxiliary storage. The algorithm takes an input stack and sorts it using a temporary stack `tmpStack`. 

   - The sorting process is done by continuously popping elements from the input stack and pushing them onto the `tmpStack` in sorted order, rearranging elements as necessary between the stacks until the original stack is empty.


> [!example] 
>  ## Code Snippet

``` run-csharp
public Stack<int> sortStack(Stack<int> input)
{
	Stack<int> tmpStack = new Stack<int>();
	
	while(input.Count > 0)
	{
		int popped = input.Pop();
		
		while(tmpStack.Count > 0 && tmpStack.Peek() > popped)
		{
			input.Push(tmpStack.Pop());
		}
		
		tmpStack.Push(popped);
	}
	return tmpStack;
}
```


![[220.7.4 Solution.svg]]















---
References: